# Architecture

> 本项目的架构设计。涉及模块拆解或组件划分时阅读。

## 模块图（文字版）

```
src/
├── api/          ← HTTP 路由层
├── core/         ← 业务逻辑（无框架依赖）
├── db/           ← 数据访问层
└── models/       ← 数据模型定义
```

## 核心数据流

[描述一个完整请求的流转路径]

## 关键依赖方向

```
api → core → db → models
api → models (直接)
```

## 设计原则

- [x] 分层职责：api 只做路由，core 只做业务
- [x] 数据模型唯一来源：models/ 中的定义
